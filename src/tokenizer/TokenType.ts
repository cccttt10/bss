enum TokenType {
    ID = 'ID',
    SPECIAL_ID = 'SPECIAL ID',
    STRING = 'STRING',
    DECIMAL = 'DECIMAL',
    SCIENTIFIC_DECIMAL = 'SCIENTIFIC DECIMAL',
    INTEGER = 'INTEGER',
    SYMBOL = 'SYMBOL',
    KEYWORD = 'KEYWORD',
    EOI = 'EOI',
}

export default TokenType;
